# child-gcs-calculator
主要修改：

修复了评分项布局问题：

使用 .scores-container 作为弹性容器，确保在大多数屏幕上三个评分项在同一行

为窄屏幕（480px以下）添加了媒体查询，确保在小屏幕上也能良好显示

添加了横向滚动支持，确保在极窄屏幕上仍可查看所有评分项

设置默认选项为"未选择"：

移除了原来设置示例值的 setTimeout 代码

在每个 updateOptions 函数中添加了代码，确保下拉菜单的默认值为空（"请选择"）

修改了选项文本，将分数放在括号内，使界面更清晰

优化了移动端适配：

改进了 .scores-container 的布局，使用 flex-wrap: nowrap 和横向滚动

为极窄屏幕（480px以下）添加了特殊处理，使三个评分项在一行内平均分布

增加了 -webkit-overflow-scrolling: touch 以改善iOS滚动体验

界面细节优化：

增加了总分数字的字体大小（2.2rem），使其更醒目

调整了评分项的间距和最小宽度

在重置按钮点击后立即更新显示
